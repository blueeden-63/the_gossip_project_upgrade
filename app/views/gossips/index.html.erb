<div class="row">
  <div class="col-md-8 mx-auto">
    <div class="alert alert-info border-0 shadow-sm mb-5">
      <h1 class="display-4 mb-3">Bienvenue sur Gossip Project!</h1>
      <p class="lead">Decouvrez les meilleurs potins du moment. Partagez, lisez, amusez-vous!</p>
    </div>

    <div class="d-flex justify-content-between align-items-center mb-4">
      <h2 class="mb-0">Tous les potins</h2>
      <%= link_to "Creer un potin", new_gossip_path, class: "btn btn-success" %>
    </div>

    <% if @gossips.any? %>
      <div class="row g-4">
        <% @gossips.each do |gossip| %>
          <div class="col-md-6">
            <div class="card h-100 shadow-sm hover-card">
              <div class="card-body">
                <span class="badge bg-dark mb-2">
                  <%= gossip.user.first_name %>
                </span>
                <h5 class="card-title"><%= gossip.title %></h5>
                <p class="card-text text-muted"><%= truncate(gossip.content, length: 100) %></p>
                <p class="mb-2">
                  <small class="text-muted"><%= gossip.comments.count %> commentaire(s)</small>
                </p>
                <small class="text-secondary">
                  <%= l(gossip.created_at, format: :short) %>
                </small>
              </div>
              <div class="card-footer bg-white border-top-0">
                <%= link_to "Lire plus", gossip_path(gossip), class: "btn btn-sm btn-primary" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-warning">
        Aucun potin pour le moment. Revenez bientot !
      </div>
    <% end %>
  </div>
</div>

<style>
  .hover-card {
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .hover-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 16px rgba(0,0,0,0.1) !important;
  }
</style>
